function distance = distanceontrack(angle,tracklength)



%angle in degrees

distanceInInches = angle/180*(tracklength*12);
distanceRounded = round(distanceInInches/(1/32))/32;

distanceRemainder = distanceRounded - round(distanceRounded);

disp()
