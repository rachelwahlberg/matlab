function [] = plotRaster()

% getting rasters for ALL trials regardless of type/artifacts present
    if isempty(plotbyfield) %if you're dividing by trials but not trial type
        

    else % if dividing trials by trial type (for ex, L vs R)
       

    end % if isempty(plotbyfield)
end % if isempty(trials)